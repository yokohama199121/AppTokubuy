import React, {Component} from "react"
import {MainBar, MyStack} from "./tabNavigation/Tabnavigator";

export default class RootStack extends Component {
    render() {
        return (
          <MyStack/>
        )
    }
}