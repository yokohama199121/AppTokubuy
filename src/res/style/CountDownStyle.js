import {
    View, StyleSheet, Text
} from 'react-native';

export default StyleSheet.create({
    viewTop: {
        height: '10%',
        backgroundColor: '#f00044',
        alignItems: 'center',
        justifyContent: 'center'
    },
});